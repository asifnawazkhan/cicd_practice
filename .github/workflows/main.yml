name: cicd practice

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: myfirst step
        run: echo "my first step"
 
  test:
    runs-on: ubuntu-22.04
    needs: build

    steps:
      - name: test
        run: echo "this is test case"
      
  deploy:
   runs-on: ubuntu-latest
   needs: test
   steps:
    - name: deploy
      run: echo "this is deploy stage"
     
          
