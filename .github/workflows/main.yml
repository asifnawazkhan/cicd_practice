name: cicd practice

on: 
  workflow_dispatch:
    inputs:
      environment:
       description: choaice the eniviroment to deploy
       required: true
       type: choice 
       default: dev
       options:
          - dev
          - test
      artifact_tag:
       description: choice the tag
       required: true
       
jobs:
 deploy:
  runs-on: ubuntu-latest

  steps:
   - name: deploy the app
     run: echo "depoly the app ${{ inputs.environment }}"
   - name: artificate
     run: echo "The artificate ${{ inputs.artifact_tag}}"

